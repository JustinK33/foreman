---
description: Find and fix backend performance bottlenecks, with measurement over guesswork.
---

Follow the `perf` skill. Scope to $ARGUMENTS if given. Look for profiling data or query logs before proposing a fix; if none exist, say so plainly. Prioritize by impact x frequency (N+1 queries and missing indexes first), and name the tradeoff for any bigger design-level change you suggest.
