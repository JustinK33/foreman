---
description: Write or update documentation that reads like an engineer wrote it, not a language model.
---

Follow the `docs` skill. Target $ARGUMENTS if given (a file, module, or doc type); otherwise infer the right artifact (README, API reference, ADR, runbook) from context and say which one you picked. Keep sentence length varied, cut throat-clearing phrases, use concrete specifics over generic superlatives, and mark real unknowns as TODOs instead of filling them with plausible-sounding filler.
