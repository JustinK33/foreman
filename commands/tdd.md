---
description: Work the current task as a strict red-green-refactor TDD loop.
argument-hint: "[function, file, or bug to scope the loop to]"
---

Follow the `tdd` skill for this task: write one small failing test, run it and confirm it fails for the right reason, write the minimum code to pass it, refactor with tests green, repeat. Don't write production code ahead of a test that demands it. If $ARGUMENTS names a specific function, file, or bug, scope the loop to that.
